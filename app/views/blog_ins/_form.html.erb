<%= form_with(model: blog_in, local: true) do |form| %>
  <% if blog_in.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog_in.errors.count, "error") %> prohibited this blog_in from being saved:</h2>

      <ul>
      <% blog_in.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
